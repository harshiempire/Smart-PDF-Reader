from app import id_to_names

def get_text(json_output): 
    
    for i in range(json_output["total_pages"]):

        elements = json_output[i] 
        for data in elements:
            
        

            return

